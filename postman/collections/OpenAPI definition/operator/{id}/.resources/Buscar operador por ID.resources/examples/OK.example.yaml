$kind: http-example
request:
  url: "{{baseUrl}}/operator/:id"
  method: GET
  headers:
    Accept: "*/*"
  pathVariables:
    id: "7007"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: "*/*"
  body:
    type: text
    content: |-
      {
        "id": 7249,
        "nombre": "string",
        "apellido": "string",
        "usuario": "string",
        "email": "string",
        "cedula": "string",
        "telefono": "string"
      }
order: 1000
